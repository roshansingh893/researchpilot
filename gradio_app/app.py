import gradio as gr

with gr.Blocks(title="ResearchPilot") as demo:
    gr.Markdown("# ResearchPilot")
    gr.Markdown(
        "Placeholder: RAG functionality will be integrated in a future phase."
    )

if __name__ == "__main__":
    demo.launch()
